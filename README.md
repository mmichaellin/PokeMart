# PokeMart

An e-commerce site built with React, Node, Express, and GraphQL.
Apollo is used for data management and interfacing with GraphQL on the frontend.
Prisma is used as a GraphQL database interface while Yoga is used for backend logic.

## Notes

* The frontend and backend have their own separate dependencies
* They also have to be run separately (Check the respective package.json files for more details)
